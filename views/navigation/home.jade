extend ../layout
mixin model_link(name, description, views, url)
    .div
        a(href='#{url}')
            h4 #{name}
        p 
            | #{description}
        p
            | #{views}
        

block content
    div
        h2 Models in the database right now: 
        -each m in models
            mixin model_link(m.name, m.description, m.views, "/models/"+m._id)